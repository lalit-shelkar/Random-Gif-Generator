import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";
//npm i axios .. secured api  calling and dont need to convert response in json format
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
 
    <App />
 
);
